spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/spring_boot_action?serverTimeZone=UTC
    username: root
    password: esmile
  quartz:
    jdbc:
      initialize-schema: always
      schema: classpath:/org/quartz/impl/jdbcjobstore/schema-mysql.sql
    job-store-type: jdbc



